# Agents

## Cursor Cloud specific instructions

This repository ("Musicify") is currently an empty/brand-new project with only a `README.md` file.

- **No source code, dependencies, or services** exist yet.
- **No build/test/lint commands** are available until the project is scaffolded.
- **Node.js** (v22) and **npm** (v10) are available in the environment.
- When the project gains a `package.json` or other dependency file, the update script should be updated accordingly.

### Starting a new project

If you need to scaffold this project, common choices include:
- `npm init` / `npx create-react-app` / `npx create-next-app` for JS/TS projects
- `cargo init` for Rust
- `poetry init` / `uv init` for Python

Update this file and the VM update script once the project structure is established.
